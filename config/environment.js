'use strict';

module.exports = function(/* environment, appConfig */) {
  let ENV = {

    fontawesome: {
      icons: {
        'free-solid-svg-icons': [
          'chevron-up',
          'chevron-down',
          'search',
          'spinner',
          'times',
        ]
      }
    },
  };

  return ENV;
};
